# QueueObject

### Extensions

| Extension | Value |
|-----------|-------|
| `spotify-docs-type` | `QueueObject` |

```jsonc
{
  "currently_playing": null,  // any, optional, The currently playing track or episode. Can be `null`.
  "queue": [  // array of object, optional
    null
  ]
}
```
